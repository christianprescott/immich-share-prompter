services:
  dev:
    build:
      context: .
      target: dev
    ports:
      - 5173:5173
    environment:
      - IMMICH_HOST
      - IMMICH_API_KEY
      - SMTP_HOST
      - SMTP_PORT
      - SMTP_USER
      - SMTP_PASS
      - SMTP_FROM
      - SMTP_TO
    volumes:
      - ./:/app/
